[package]
name = "emulator"
version = "0.1.0"
edition = "2024"

[dependencies]
minifb = "0.10.7"
clap = "2.32.0"
serde = { version = "1", optional = true }
serde_derive = { version = "1", optional = true }

[features]
serialize = ["serde", "serde_derive"]
